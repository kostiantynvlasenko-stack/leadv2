# Project-specific lead rules — GENERIC STUB

## Stack (fill in after leadv2-init)

- TODO: describe your stack (lang, DB, hosting, deploy method)
- TODO: add any custom review rules (e.g. "all API changes need auth review")
- TODO: add deploy quirks (env var requirements, first-time setup steps)

## Required env

```sh
# Set these before running deploy.sh / verify.sh:
export LEAD_V2_TASK_ID=<injected-by-lead>
# TODO: add your project-specific env vars here
```

## Known limitations

- deploy.sh and verify.sh are stubs — they will fail until you fill them in.
- See examples/overrides/ in the leadv2 plugin for working templates.
