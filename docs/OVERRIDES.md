# Override Configuration

> Canonical source: [`plugins/leadv2/docs/OVERRIDES.md`](../plugins/leadv2/docs/OVERRIDES.md)
>
> This file is a redirect stub. All override documentation — including `stack.yaml`,
> `deploy.sh`, `verify.sh`, `codex-policy.yaml`, `state-paths.yaml`, `extensions.md`,
> `active-limits.yaml`, and new keys (`src_roots`, `hot_paths`, `migration_glob`) —
> lives in the plugin docs so it ships to external users.
