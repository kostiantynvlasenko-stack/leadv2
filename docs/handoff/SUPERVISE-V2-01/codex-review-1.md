- High — `leadv2_ask_async` creates an unrelated control-plane QID and then returns/waits on the legacy QID, so answering the V2 question surfaced by the supervisor cannot wake the child while answering the legacy question leaves a permanent V2 pending duplicate, directly violating the single-store/no-new-legacy-writers drain contract (`plugins/leadv2/scripts/leadv2-helpers.sh:2253`).
- High — Fanout bypasses `leadv2_active_register` and appends rows without any of the new `protocol_version`, `backend`, `phase_started_at`, `updated_at`, tmux, log-path, or provider-receipt fields, so the primary child-launch path defeats the registry-honesty migration (`plugins/leadv2/scripts/leadv2-fanout.sh:532`).
- High — On stock macOS, where neither GNU `gtimeout` nor `timeout` is installed, the wrapper silently runs Node without any deadline, making tier-aware `CODEX_TIMEOUT` and the exit-124 event completely inert (`plugins/leadv2/scripts/codex-task.sh:204`).
- High — A real timeout exits 124 immediately and `_run_with_fallback` retries only 5.6 HTTP-400 failures, so the required automatic lower-tier retry never occurs and the review is simply abandoned (`plugins/leadv2/scripts/codex-task.sh:218`).
- High — The supervisor silently discards any unreadable or malformed control-plane question, allowing a blocked child to disappear from `requires_founder` with no warning or non-zero status (`plugins/leadv2/scripts/leadv2-supervise.sh:305`).
