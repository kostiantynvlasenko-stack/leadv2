- High — Entering supervisor mode writes `.supervise-active`, whose installed guard denies every non-Explore `Agent` invocation from that same session, so the rewritten D-f=A flow's default background-Agent lanes cannot be launched at all (`plugins/leadv2/scripts/leadv2-supervise.sh:106`; `plugins/leadv2/skills/leadv2-supervise/SKILL.md:46`).
- High — `--ensure` performs an unlocked check-then-create and its EXIT trap unconditionally removes the shared sentinel, so concurrent re-entry can start duplicate loops and either loop can erase the other's ownership record (`plugins/leadv2/scripts/leadv2-supervise-loop.sh:99`; `plugins/leadv2/scripts/leadv2-supervise-loop.sh:152`).
- High — Tmux death advances when any single reason is present instead of requiring both a missing window/pane and a dead or birth-mismatched PID, so a live child with a transiently missing tmux window is pruned after two calls in violation of D-d and the live-child off-limits constraint (`plugins/leadv2/scripts/leadv2-supervise.sh:516`).
- High — The mutation replaces `active.yaml` with dead rows already removed before it even opens the tombstone file, so a tombstone write failure or crash permanently prunes the row without the required prior tombstone (`plugins/leadv2/scripts/leadv2-supervise.sh:577`).
- High — `dead_now` bypasses `new_events` filtering and is returned on every observe-only delta call, causing the loop to append the same DEAD urgent line every five seconds and violating the deduplicated-event/no-unchanged-chat pulse ceiling (`plugins/leadv2/scripts/leadv2-supervise.sh:940`).
